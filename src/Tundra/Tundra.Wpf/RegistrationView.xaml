<Page x:Class="Tundra.Wpf.RegistrationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="RegistrationView"
      DataContext="{Binding RegistrationViewModel, Source={StaticResource Bootstrapper}}">

    <Grid Background="Black">
        <Button Content="Back" Foreground="BlueViolet" HorizontalAlignment="Center" VerticalAlignment="Center"
                MinHeight="30" MinWidth="100"
                Command="{Binding GoBack}" />
    </Grid>
</Page>